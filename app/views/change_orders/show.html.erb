<%= content_for :title, "Change Orders ##{@change_order.id}" %>
<%= turbo_stream_from @change_order %>

<div class="container px-4 mx-auto my-8">
  <div class="flex items-center justify-between mb-4">
    <h1 class="h3">
      <%= link_to "Change Orders", change_orders_path, class: "text-black" %>
      <span class="text-gray-400 font-light mx-2">\</span>
      Change Order #<%= @change_order.id %>
    </h1>
    <%= link_to t("edit"), edit_change_order_path(@change_order), class: "btn btn-white" %>
  </div>

  <div class="p-8 bg-white rounded shadow">
    <%= render @change_order %>
  </div>
</div>
