<%= content_for :title, "Temp Crews ##{@temp_crew.id}" %>
<%= turbo_stream_from @temp_crew %>

<div class="container px-4 mx-auto my-8">
  <div class="flex items-center justify-between mb-4">
    <h1 class="h3">
      <%= link_to "Temp Crews", temp_crews_path, class: "text-black" %>
      <span class="text-gray-400 font-light mx-2">\</span>
      Temp Crew #<%= @temp_crew.id %>
    </h1>
    <%= link_to t("edit"), edit_temp_crew_path(@temp_crew), class: "btn btn-white" %>
  </div>

  <div class="p-8 bg-white rounded shadow">
    <%= render @temp_crew %>
  </div>
</div>
