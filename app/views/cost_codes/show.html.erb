<%= content_for :title, "Cost Codes ##{@cost_code.id}" %>
<%= turbo_stream_from @cost_code %>

<div class="container px-4 mx-auto my-8">
  <div class="flex items-center justify-between mb-4">
    <h1 class="h3">
      <%= link_to "Cost Codes", cost_codes_path, class: "text-black" %>
      <span class="text-gray-400 font-light mx-2">\</span>
      Cost Code #<%= @cost_code.id %>
    </h1>
    <%= link_to t("edit"), edit_cost_code_path(@cost_code), class: "btn btn-white" %>
  </div>

  <div class="p-8 bg-white rounded shadow">
    <%= render @cost_code %>
  </div>
</div>
