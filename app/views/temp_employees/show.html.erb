<%= content_for :title, "Temp Employees ##{@temp_employee.id}" %>
<%= turbo_stream_from @temp_employee %>

<div class="container px-4 mx-auto my-8">
  <div class="flex items-center justify-between mb-4">
    <h1 class="h3">
      <%= link_to "Temp Employees", temp_employees_path, class: "text-black" %>
      <span class="text-gray-400 font-light mx-2">\</span>
      Temp Employee #<%= @temp_employee.id %>
    </h1>
    <%= link_to t("edit"), edit_temp_employee_path(@temp_employee), class: "btn btn-white" %>
  </div>

  <div class="p-8 bg-white rounded shadow">
    <%= render @temp_employee %>
  </div>
</div>
