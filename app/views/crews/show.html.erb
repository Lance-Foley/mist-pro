<%= content_for :title, "Crews ##{@crew.id}" %>
<%= turbo_stream_from @crew %>

<div class="container px-4 mx-auto my-8">
  <div class="flex items-center justify-between mb-4">
    <h1 class="h3">
      <%= link_to "Crews", crews_path, class: "text-black" %>
      <span class="text-gray-400 font-light mx-2">\</span>
      Crew #<%= @crew.id %>
    </h1>
    <%= link_to t("edit"), edit_crew_path(@crew), class: "btn btn-white" %>
  </div>

  <div class="p-8 bg-white rounded shadow">
    <%= render @crew %>
  </div>
</div>
